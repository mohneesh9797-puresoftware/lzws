language: c

os:
  - linux
  - osx

dist: xenial

compiler:
  - gcc
  - clang

addons:
  apt:
    packages:
      - libgmp-dev
  homebrew:
    packages:
      - gmp

script:
  - scripts/toolchains.sh
