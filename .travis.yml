language: c

os:
  - linux
  - osx

arch:
  - amd64
  - arm64

addons:
  apt:
    packages:
      - libgmp-dev
    update: true
  homebrew:
    packages:
      - gmp
    update: true

script:
  - scripts/toolchains.sh
